name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release-smith:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build smith binaries
        run: make dist GO=go

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/smith-linux-amd64
            dist/smith-linux-arm64
            dist/smith-darwin-amd64
            dist/smith-darwin-arm64
            dist/smith-windows-amd64.exe
          draft: false
          prerelease: false
          generate_release_notes: true
