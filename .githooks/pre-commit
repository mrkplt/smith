#!/usr/bin/env bash
set -euo pipefail

if [[ "${SKIP_GIT_HOOKS:-}" == "1" ]]; then
  exit 0
fi

if [[ "${SMITH_HOOKS_IN_DOCKER:-1}" == "1" ]] && command -v docker >/dev/null 2>&1; then
  "$(dirname "$0")/run-in-docker.sh" hooks-run-pre-commit
else
  make hooks-run-pre-commit
fi
