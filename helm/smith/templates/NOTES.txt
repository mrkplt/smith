Smith chart installed.

API service:
  {{ include "smith.fullname" . }}-api:{{ .Values.api.service.port }}

Console service:
  {{ include "smith.fullname" . }}-console:{{ .Values.console.service.port }}

Chat service:
  {{ include "smith.fullname" . }}-chat:{{ .Values.chat.service.port }}
